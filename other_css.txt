* Transparent input fields */
.transparent-field {
    background-color: transparent;
    border: 1px solid rgba(255, 255, 255, 0.7);
    color: white;
    padding: 10px;
    border-radius: 5px;
    outline: none;
}

.transparent-field:focus {
    border-color: #00bcd4; /* Highlight color on focus */
}

/* Style for buttons in links */
.btn-link-style {
    text-decoration: none; /* Remove underline */
    display: inline-block;
    padding: 10px 20px;
    background-color: #007bff; /* Bootstrap primary color */
    color: white;
    border-radius: 5px;
    font-weight: bold;
    text-align: center;
    transition: background-color 0.3s ease;
}

.btn-link-style:hover {
    background-color: #0056b3; /* Darker shade for hover effect */
    text-decoration: none;
}
/* Transparent card body */
.card {
    background-color: rgba(255, 255, 255, 0.2); /* Semi-transparent white */
    border: none; /* Optional: Remove border for a cleaner look */
    border-radius: 10px; /* Rounded edges */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Subtle shadow effect */
    overflow: hidden; /* Ensures content stays within the card */
}

.card-body {
    background-color: transparent; /* Fully transparent background */
    color: white; /* Ensures text is visible */
    padding: 20px; /* Add spacing inside the card body */
}

/* Card hover effect */
.card:hover {
    transform: translateY(-5px); /* Subtle lift effect */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3); /* Enhanced shadow on hover */
    transition: all 0.3s ease-in-out;
}

/* Card title styling */
.card-title {
    font-size: 1.5rem;
    font-weight: bold;
}

/* Card text styling */
.card-text {
    font-size: 1rem;
    line-height: 1.5;
}

/* Buttons inside the card */
.card .btn-link-style {
    display: inline-block;
    margin-top: 10px;
}
/* Form layout container */
.form-layout {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

/* Individual form group styling */
.form-group {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: 100%;
    margin-bottom: 15px;
}

/* Label styling */
.form-group label {
    margin-bottom: 5px;
    font-size: 1rem;
    font-weight: bold;
    color: white; /* Ensure label text is visible */
    text-align: left;
}

/* Transparent fields styling */
.transparent-field,
.transparent-textarea {
    background-color: transparent;
    border: 1px solid rgba(255, 255, 255, 0.7);
    color: white;
    padding: 10px;
    border-radius: 5px;
    outline: none;
    width: 100%; /* Ensure fields take up full width */
    margin-bottom: 5px; /* Space below the field */
    font-size: 1rem;
}

/* Focus effect */
.transparent-field:focus,
.transparent-textarea:focus {
    border-color: #00bcd4;
    box-shadow: 0 0 5px #00bcd4;
}

/* Textarea specific styling */
.transparent-textarea {
    resize: none; /* Prevent resizing */
    height: 100px; /* Adjust height as needed */
}

/* Help text styling */
.form-text {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.7); /* Subtle color for help text */
}

/* Button group styling */
.button-group {
    display: flex;
    justify-content: space-between;
    gap: 10px;
}

/* Buttons styling inside the form */
.button-group button {
    flex: 1;
    padding: 10px 20px;
    border: none;
    background-color: transparent;
    color: white;
    font-size: 1.1em;
    transition: background-color 0.3s ease, color 0.3s ease;
}

/* Button hover effect */
.button-group button:hover {
    background-color: white;
    color: black;
}

input.transparent-textarea:-webkit-autofill,
input.transparent-textarea:-webkit-autofill:hover,
input.transparent-textarea:-webkit-autofill:focus,
input.transparent-textarea:-webkit-autofill:active {
    background-color: transparent !important;
    color: white !important;
    -webkit-box-shadow: 0 0 0px 1000px transparent inset !important;
    box-shadow: 0 0 0px 1000px transparent inset !important;
}

input.transparent-textarea:focus {
    background-color: transparent !important;
    border-color: #00bcd4;
    box-shadow: 0 0 5px #00bcd4;
}
input.transparent-textarea:focus,
textarea.transparent-textarea:focus {
    background-color: transparent !important;
    color: white !important;
    border: 1px solid #00bcd4;
    box-shadow: 0 0 5px #00bcd4;
}
